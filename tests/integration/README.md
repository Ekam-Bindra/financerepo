# Integration tests

Cross-service tests will live here. Unit tests for the frontend and API run
without Docker. Integration tests should use the Docker Compose services and
verify PostgreSQL, Redis, API readiness, and frontend health together.

